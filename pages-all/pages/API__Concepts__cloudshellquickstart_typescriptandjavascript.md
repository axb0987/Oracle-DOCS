# SDK for TypeScript and JavaScript Cloud Shell Quick Start
- Source: https://docs.oracle.com/en-us/iaas/Content/API/Concepts/cloudshellquickstart_typescriptandjavascript.htm
- Fetched: 2026-09-05 01:35 CDT

# SDK for TypeScript and JavaScript Cloud Shell Quick Start

This quick start shows you how to quickly get started running sample code with the Oracle Cloud Infrastructure SDK for TypeScript and JavaScript using Cloud Shell.

The OCI SDK for TypeScript and JavaScript SDK is pre-installed globally through npm.

JavaScript Example
- Sign in to the Console.
- Click the Cloud Shell icon in the Console header. Note that Cloud Shell runs commands against the region selected in the Console's Region selection menu when Cloud Shell was started.
- Create a file named`region_subscriptions_example.js`with the following example code, which lists the subscribed regions for the current tenancy:

```

```

- Create a file named`region_subscriptions_example.js`with the following example code, which lists the subscribed regions for the current tenancy:
```

```

- Run the example:
```

```

TypeScript Example
This TypeScript example lists the subscribed regions for the current tenancy.
- Create an empty project named ts_demo and link the global installation of oci-sdk library using the following commands:

```

```

- Create a file named`region_subscriptions_example.ts`inside the ts_demo project with the following code:

```

```

- Compile the example:

```

```

- Run the example:

```

```
