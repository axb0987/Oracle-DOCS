# Key Management Crypto Functions
- Source: https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html
- Fetched: 2026-09-05 19:08 CDT

### [Oracle Cloud Infrastructure Documentation](https://docs.oracle.com/iaas/Content/home.htm)

All Pages

[Skip to main content](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#dcoc-content-body)

## Key Management Crypto Functions

Package: DBMS_CLOUD_OCI_KM_KMS_CRYPTO

### DECRYPT Function

Decrypts data using the given[DecryptDataDetails](https://docs.oracle.com/iaas/api/#/en/key/latest/datatypes/DecryptDataDetails)resource.

Syntax
```

```

Parameters

Parameter Description

`decrypt_data_details`

(required) DecryptDataDetails

`opc_request_id`

(optional) Unique identifier for the request. If provided, the returned request ID will include this value. Otherwise, a random request ID will be generated by the service.

`region`

(optional) OCI region id. e.g us-phoenix-1 for US West (Phoenix).

`endpoint`

(optional) The endpoint of the service to call using this function. e.g https://kms.{region}.{secondLevelDomain}.If both endpoint and region are given, then endpoint takes precedence.

`credential_name`

(optional) The name of the credential for authenticating with the corresponding cloud native API.

### ENCRYPT Function

Encrypts data using the given[EncryptDataDetails](https://docs.oracle.com/iaas/api/#/en/key/latest/datatypes/EncryptDataDetails)resource. Plaintext included in the example request is a base64-encoded value of a UTF-8 string.

Syntax
```

```

Parameters

Parameter Description

`encrypt_data_details`

(required) EncryptDataDetails

`opc_request_id`

(optional) Unique identifier for the request. If provided, the returned request ID will include this value. Otherwise, a random request ID will be generated by the service.

`region`

(optional) OCI region id. e.g us-phoenix-1 for US West (Phoenix).

`endpoint`

(optional) The endpoint of the service to call using this function. e.g https://kms.{region}.{secondLevelDomain}.If both endpoint and region are given, then endpoint takes precedence.

`credential_name`

(optional) The name of the credential for authenticating with the corresponding cloud native API.

### EXPORT_KEY Function

Exports a specific version of a master encryption key according to the details of the request. For their protection, keys that you create and store on a hardware security module (HSM) can never leave the HSM. You can only export keys stored on the server. For export, the key version is encrypted by an RSA public key that you provide. This operation is not supported for keys having protection mode `EXTERNAL`.

Syntax
```

```

Parameters

Parameter Description

`export_key_details`

(required) ExportKeyDetails

`region`

(optional) OCI region id. e.g us-phoenix-1 for US West (Phoenix).

`endpoint`

(optional) The endpoint of the service to call using this function. e.g https://kms.{region}.{secondLevelDomain}.If both endpoint and region are given, then endpoint takes precedence.

`credential_name`

(optional) The name of the credential for authenticating with the corresponding cloud native API.

### GENERATE_DATA_ENCRYPTION_KEY Function

Generates a key that you can use to encrypt or decrypt data.

Syntax
```

```

Parameters

Parameter Description

`generate_key_details`

(required) GenerateKeyDetails

`opc_request_id`

(optional) Unique identifier for the request. If provided, the returned request ID will include this value. Otherwise, a random request ID will be generated by the service.

`region`

(optional) OCI region id. e.g us-phoenix-1 for US West (Phoenix).

`endpoint`

(optional) The endpoint of the service to call using this function. e.g https://kms.{region}.{secondLevelDomain}.If both endpoint and region are given, then endpoint takes precedence.

`credential_name`

(optional) The name of the credential for authenticating with the corresponding cloud native API.

### SIGN Function

Creates a digital signature for a message or message digest by using the private key of a public-private key pair, also known as an asymmetric key. To verify the generated signature, you can use the[Verify](https://docs.oracle.com/iaas/api/#/en/key/latest/VerifiedData/Verify)operation. Or, if you want to validate the signature outside of the service, you can do so by using the public key of the same asymmetric key. This operation is not supported for keys having protection mode `EXTERNAL`.

Syntax
```

```

Parameters

Parameter Description

`sign_data_details`

(required) SignDataDetails

`opc_request_id`

(optional) Unique identifier for the request. If provided, the returned request ID will include this value. Otherwise, a random request ID will be generated by the service.

`region`

(optional) OCI region id. e.g us-phoenix-1 for US West (Phoenix).

`endpoint`

(optional) The endpoint of the service to call using this function. e.g https://kms.{region}.{secondLevelDomain}.If both endpoint and region are given, then endpoint takes precedence.

`credential_name`

(optional) The name of the credential for authenticating with the corresponding cloud native API.

### VERIFY Function

Verifies a digital signature that was generated by the[Sign](https://docs.oracle.com/iaas/api/#/en/key/latest/SignedData/Sign)operation by using the public key of the same asymmetric key that was used to sign the data. If you want to validate the digital signature outside of the service, you can do so by using the public key of the asymmetric key. This operation is not supported for keys having protection mode `EXTERNAL`.

Syntax
```

```

Parameters

Parameter Description

`verify_data_details`

(required) VerifyDataDetails

`opc_request_id`

(optional) Unique identifier for the request. If provided, the returned request ID will include this value. Otherwise, a random request ID will be generated by the service.

`region`

(optional) OCI region id. e.g us-phoenix-1 for US West (Phoenix).

`endpoint`

(optional) The endpoint of the service to call using this function. e.g https://kms.{region}.{secondLevelDomain}.If both endpoint and region are given, then endpoint takes precedence.

`credential_name`

(optional) The name of the credential for authenticating with the corresponding cloud native API.

- [Key Management Crypto Functions](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-EC73AF81-72A1-47F6-8F52-377097FDFE66)
- [DECRYPT Function](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-90EC3CD2-F38E-4AF3-AE13-ABE361FD53E3)
- [ENCRYPT Function](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-A0CE1922-29CE-4D7A-AEFF-BAE911438B45)
- [EXPORT_KEY Function](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-390EE170-CCF5-4BFA-AB10-ECB9C125D1A5)
- [GENERATE_DATA_ENCRYPTION_KEY Function](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-0DAF4D02-23AA-4448-8191-14D855130ADA)
- [SIGN Function](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-9C6D1B22-8D37-448E-B087-A71E0E431507)
- [VERIFY Function](https://docs.oracle.com/iaas/Content/pl-sql-sdk/doc/dbms_cloud_oci_km_kms_crypto.html#ADSDK-GUID-02A1BB19-E7E0-4633-986A-3BACBD24506A)

- [About Oracle](https://www.oracle.com/corporate/index.html)
- [Contact Us](https://www.oracle.com/corporate/contact/index.html)
- [Legal Notices](https://docs.oracle.com/iaas/Content/legalnotices.htm)
- [Terms of Use &amp; Privacy](https://www.oracle.com/legal/privacy/)
- [Document Conventions](https://docs.oracle.com/iaas/Content/General/Reference/docconventions.htm)
-
